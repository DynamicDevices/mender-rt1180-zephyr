# FRDM-IMXRT1186 Spectra 6 SPI lab (CM33).
# Build: ./scripts/build-rt1186-frdm-eink.sh
#
# No full framebuffer: 1200×1600 L_4 is 960 KiB; stream via the EL133 driver.
# Not a battery / product profile.

CONFIG_APP_EINK=y
CONFIG_APP_EINK_SHELL=y
CONFIG_APP_EINK_FULL_FRAMEBUFFER=n
CONFIG_APP_EINK_DISPLAY_LCD_PREVIEW=n
CONFIG_APP_EINK_BATTERY_DUTY_CYCLE=n
# e-tabelone over NETC (same Ethernet as Hosted Mender). Sync stays off until
# `eink creds` + `eink sync` (or set APP_EINK_HTTP_ENABLE). 32 KiB JSON arena —
# default 256 KiB BSS will not fit with HyperRAM off.
CONFIG_APP_EINK_HTTP=y
CONFIG_APP_EINK_HTTP_JSON_ARENA_SIZE=32768
CONFIG_APP_EINK_HTTP_ENABLE=n
CONFIG_APP_EINK_HTTP_V2_SYNC=y
CONFIG_APP_EINK_HTTP_BASE_URL="https://etablone.dynamicdevices.co.uk"
CONFIG_APP_EINK_RESOURCE_TELEMETRY=y
CONFIG_APP_EINK_LZ4=y
CONFIG_SNTP=y
CONFIG_APP_EINK_HTTP_SNTP_SERVER="pool.ntp.org"
CONFIG_APP_EINK_LOCATION=n
CONFIG_APP_EINK_IW612_GPIO=n
CONFIG_APP_EINK_PANEL_GPIO=n
CONFIG_APP_EINK_STORE_ROOT="/lfs1/eink"

CONFIG_DISPLAY=y
CONFIG_SPI=y
CONFIG_GPIO=y

# APP_EINK store/display/location call fs_*; shell fragment wants FILE_SYSTEM_SHELL.
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_FLASH_PAGE_LAYOUT=y
CONFIG_FILE_SYSTEM=y
CONFIG_FILE_SYSTEM_LITTLEFS=y

# HTTP + Mender TLS share the system heap; bump above Mender-only lab default.
CONFIG_HEAP_MEM_POOL_SIZE=98304
CONFIG_MAIN_STACK_SIZE=8192

# Lab UART: see netup/DHCP on MCU-Link without waiting for the deferred log thread.
CONFIG_LOG_MODE_DEFERRED=n
CONFIG_LOG_MODE_IMMEDIATE=y
